name: hw1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  # 설치된 Flutter가 사용하는 Dart 버전에 맞춰주세요.
  # (안 맞으면 pub get 실패) 보편적으로 이렇게 두면 안전합니다.
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/lake.jpg
